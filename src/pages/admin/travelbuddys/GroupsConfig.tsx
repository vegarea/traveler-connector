import React from 'react';
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

const GroupsConfig = () => {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Configuración de Grupos</CardTitle>
      </CardHeader>
      <CardContent>
        <p>Configuración de grupos de TravelBuddies</p>
      </CardContent>
    </Card>
  );
};

export default GroupsConfig;